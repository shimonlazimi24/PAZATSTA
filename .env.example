# Database (required). Use pooler URL for serverless (e.g. Netlify); direct for local/migrations.
# Supabase: Project Settings → Database → Connection string (URI). Use "Transaction" pooler for serverless.
# Example direct: postgresql://postgres:YOUR_PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres
# Example pooler: postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres
DATABASE_URL="postgresql://user:password@host:5432/postgres"
RESEND_API_KEY="re_xxxx"
RESEND_FROM="info@pazatsta.co.il"
APP_URL="http://localhost:3000"
COOKIE_SECRET="min-32-chars-random-string-for-signing"

# Contact (shown in footer). Optional: NEXT_PUBLIC_CONTACT_EMAIL, NEXT_PUBLIC_CONTACT_PHONE
# Admin user for seed: admin@pazatsta.co.il
NEXT_PUBLIC_CONTACT_EMAIL="info@pazatsta.co.il"
NEXT_PUBLIC_CONTACT_PHONE="050-2632320"

# PDF storage (default: ./storage/pdfs). For Vercel use Vercel Blob or similar.
STORAGE_PATH="./storage/pdfs"

# Cron: set a secret and call GET /api/cron/follow-up with Authorization: Bearer <CRON_SECRET>
CRON_SECRET=""
